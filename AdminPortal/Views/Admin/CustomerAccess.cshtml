@model CustomerAccessViewModel;

@{
    ViewData["Title"] = "Customer Access";
}
<h1>@ViewData["Title"]</h1>
<hr>
<div class="container">
    <div class="row">
        <partial name="Admin/_CustomerAccessForm" model="@Model" />
    </div>
</div>
<hr>

@if (Model.Response != null)
{
    <h3 class="text-danger">@Model.Response</h3>
}

